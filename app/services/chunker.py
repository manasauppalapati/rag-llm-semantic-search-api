"""
Chunking service for converting raw document text into retrieval units.

When implemented, this module will split loaded text into token-aware chunks
with overlap and metadata for exact source attribution.
Phase 3 — Processing.
Depends on: app.core.config, app.models.schemas.
Depended on by: app.api.routes.ingest, app.services.embeddings.
"""


def _placeholder() -> None:
    raise NotImplementedError("Implementation pending — Phase 3")
