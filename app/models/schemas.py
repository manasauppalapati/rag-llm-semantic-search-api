"""
Pydantic schema definitions for API requests, responses, and shared models.

When implemented, this module will define validation contracts for ingest,
query, admin, health, and job status payloads across the application.
Phase 1 — Foundation.
Depends on: app.core.config.
Depended on by: app.api.routes.ingest, app.api.routes.query,
app.api.routes.admin, app.api.routes.health, app.services.chunker,
app.services.rag_pipeline.
"""


def _placeholder() -> None:
    raise NotImplementedError("Implementation pending — Phase 1")
